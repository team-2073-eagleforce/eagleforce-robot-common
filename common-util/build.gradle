plugins {
    id 'java'
    id "eclipse"
    id "idea"
    id "maven-publish"
}
apply plugin: 'org.junit.platform.gradle.plugin'
apply plugin: 'kotlin'

dependencies {
    compile group: 'commons-io', name: 'commons-io', version: '2.5'
}